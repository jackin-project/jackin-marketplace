# CLAUDE.md

See [AGENTS.md](AGENTS.md) for shared agent instructions.

This is a **Claude Code plugin marketplace manifest**. Every plugin listed in `.claude-plugin/marketplace.json` runs inside downstream users' Claude Code sessions with full tool access. Plugin additions from outside `jackin-project/*` require documented trust rationale. Every `source.url` change is a trust break.

This repository uses `main` as its primary branch.
